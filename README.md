# cypher-X AI 🐉

A free multimodal AI (text + image) built with vLLM + HuggingFace.

## 🚀 Setup
```bash
git clone https://github.com/cyrushmf69/cypherx.git
cd cypher-x
pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 7860 --reload
